import {Text, View} from "react-native";

const BarSelection = () => {
    const [BarList, setBarList] = useState([])
    return (
        <View>
            <Text>Bar</Text>
        </View>
    );
};

export default BarSelection;
